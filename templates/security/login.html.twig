{% extends 'layout.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <h1 class="h3 mb-3 font-weight-normal">Please sign in</h1>
    <label for="inputEmail" class="sr-only">Email</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" placeholder="Email" required autofocus>
    <label for="inputPassword" class="sr-only">Password</label>
    <input type="password" name="password" id="inputPassword" class="form-control" placeholder="Password" required>

    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
    >

    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}

    <button class="btn btn-lg btn-primary" type="submit">
        Sign in
    </button>
</form>
<br>
<div class="bg-secondary text-light">
Aplication Movie Drinking Game<br>
- generation des fixture pour les entités de l'application:<br>
  php bin/console hautelook:fixtures:load<br>
- pour se connecter :<br>
    -email admin: admin@admin.com<br>
    -mot de passe :najv<br>

Coté admin :<br>
    -accés à la gestion session , regle, film , participant réservée à l'admin <br>
    -utilisation de api plattform<br>
    -tableau de bord ajax (page home) <br>
    -utilisation de easyadmin pour la gestion des participants et matchs<br>     
<br>
ou s'inscrire dans l'onglet inscription de l'application<br>

Depôt Git branch s4b : <a href="https://github.com/nicovernot/Symfo4.git">https://github.com/nicovernot/Symfo4.git</a> <br>

</div>




{% endblock %}
