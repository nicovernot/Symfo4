{% extends 'layout.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
{{dump(list)}}


</div>


<div class="container">
 <div class="row">
  <div class="col">
  <h2>Striped Rows</h2>
  <p>The .table-striped class adds zebra-stripes to a table:</p>            
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Session</th>
        <th>Lien</th>
       
      </tr>
    </thead>
    <tbody>
      <tr>
       
 {% for liste in list %}
      </tr>
 <td> {{liste.session.nom~"  "~liste.session.date|date("m/d/Y") }}</td>
 <td> <a class="btn btn-info" href="#">Participer</a><br></td>
 </tr>
 {%  endfor %}
    </tbody>
  </table>
  
  </div>
  <div class="col">
    <h2>Striped Rows</h2>
  <p>The .table-striped class adds zebra-stripes to a table:</p>            
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Session</th>
        <th>Lien</th>
       
      </tr>
    </thead>
    <tbody>
      <tr>
       
 {% for liste in list %}
      </tr>
 <td> {{liste.session.nom~"  "~liste.session.date|date("m/d/Y") }}</td>
 <td> <a class="btn btn-info" href="#">Participer</a><br></td>
 </tr>
 {%  endfor %}
    </tbody>
  </table>
  </div>
 
</div> 
</div>
{% endblock %}
